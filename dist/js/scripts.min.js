const toggleMenu=document.querySelector(".toggle"),menu=document.querySelector(".menu__list");function toggleFunction(e){this&&menu.classList.toggle("menu__list-is-active")}toggleMenu.addEventListener("click",toggleFunction);